/**
 * main - starting point
 * @argc: number of arguments
 * @argv: array pointing to argumentgs
 * Return: Always (0) success
 */

#include "main.h"
#include <stdio.h>

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);
	return (0);
}
